# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
/coverage
/test-results
/playwright-report
/playwright/.cache

# Production
/dist
/build

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor directories and files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
Thumbs.db

# Claude Code agent tooling (local only)
.claude/

# SDD scratch workspace (local only)
.superpowers/

# Specs and implementation plans (local only). Kept on disk because they are
# still useful while working, but deliberately not versioned in this public
# repo.
docs/superpowers/

# Generated design system source of truth (local only)
design-system/

# TypeScript incremental build info (tsc -b)
*.tsbuildinfo

# Local tooling scan output (never belongs in this repo)
karpathy-report.json

# Resume working files (local only). These carry personal contact details
# and unpublished drafts; only the rendered PDF in public/ is versioned.
resume/
